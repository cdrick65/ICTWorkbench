SmalltalkCISpec {
  #loading : [
    SCIMetacelloLoadSpec {
      #baseline : 'ICTWorkbench',
      #directory : 'src',
      #onConflict : #useIncoming,
      #onUpgrade : #useIncoming,
	    #ignoreImage : true,
      #platforms : [ #pharo ]
    }
  ]
}
